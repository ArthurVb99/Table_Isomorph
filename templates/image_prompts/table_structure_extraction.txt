You are a vision-language model specialized in table structure recognition and data extraction.

The attached image contains a table with data arranged in rows and columns.

Your task is to:
1. Analyze the table structure in the image
2. Extract all visible text content from each cell
3. Identify the bounding boxes (bbox) for each cell in the format [x1, y1, x2, y2]
4. Generate the HTML table structure tokens that represent the table layout
5. Return the complete table structure as valid JSON

Requirements:
- Extract ALL visible text as individual tokens (words/characters) in each cell
- Include accurate bounding box coordinates [x1,y1,x2,y2] for each cell
- Generate proper HTML table structure tokens (<thead>, <tr>, <td>, </td>, </tr>, </thead>, <tbody>, etc.)
- Maintain the exact table layout and hierarchy
OUTPUT CONTRACT:
- Return exactly one JSON object and nothing else.
- Do not return Markdown, code fences, explanations, comments, or trailing text.
- The JSON object MUST have exactly these top-level keys: "imgid", "html", "split", "filename".
- "html" MUST have exactly these keys: "cells" and "structure".
- "cells" MUST be an array of objects. Every cell object MUST have exactly two keys:
  "tokens" and "bbox".
- "tokens" MUST be an array of strings. Use [] when a cell has no visible text.
- "bbox" MUST be an array of exactly four integers: [x1, y1, x2, y2].
- "structure" MUST be an object with exactly one key: "tokens".
- "structure.tokens" MUST be an array of strings containing the complete HTML structure.
- NEVER make "structure" a bare array or a string.
- NEVER put "bbox" outside its cell object.
- Do not use placeholders such as x1, y1, ..., or "list of tokens".
- Use the supplied image filename for "filename" and "train" for "split".

Return JSON matching this schema exactly:
JSON Structure:
{
  "imgid": 1,
  "html": {
    "cells": [
      {
        "tokens": ["list", "of", "text", "tokens", "in", "this", "cell"],
        "bbox": [x1, y1, x2, y2]
      }
    ],
    "structure": {
      "tokens": ["<thead>", "<tr>", "<td>", "</td>", "<td>", "</td>", "</tr>", "</thead>", "<tbody>", "..."]
    }
  },
  "split": "train",
  "filename": "image.png"
}

Important:
- Coordinates should be integers representing pixel positions
- Cell tokens should be individual words/characters as they appear in the table
- Structure tokens should represent the complete HTML table markup
- Ensure all cells are captured with their exact content and positions

Before finishing, check that every opening and closing brace, bracket, and quote is present; every cell has a bbox; and "html.structure" is an object containing a "tokens" array.